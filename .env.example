# Copy to .env and fill in. .env is gitignored.
# Create a key in Google Cloud Console with the "Places API (New)" enabled,
# on a project with billing on. A dedicated key (not the Beija/AMS key) keeps
# this side project's spend off Beija's billing.
GOOGLE_PLACES_API_KEY=
